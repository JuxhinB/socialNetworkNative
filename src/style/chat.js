import { StyleSheet } from 'react-native';

export const chat = StyleSheet.create({
    label:{
        color:'black',
        fontSize: 16,
        height:30,
        textAlign : 'center',
        marginTop:10,
        marginBottom:10
    }
});